{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build wayland-ssh-askpass",
            "type": "shell",
            "command": "meson compile -C \"${workspaceFolder}/build\" wayland-ssh-askpass",
            "group": "build",
        }
    ]
}